# ==============================
# ตัวอย่างตัวแปรแวดล้อม (คัดลอกไปเป็น .env แล้วใส่ค่าจริง)
# ห้าม push ไฟล์ .env จริงขึ้น repository
# ==============================

# --- Database (PostgreSQL) ---
DB_HOST=db
DB_PORT=5432
DB_USER=staffcar_user
DB_PASSWORD=changeme
DB_NAME=staffcar

# --- Backend ---
PORT=3000
NODE_ENV=development

# --- Frontend ---
# URL ของ backend API ที่ frontend เรียกใช้
API_BASE_URL=http://localhost:3000/api
# หมายเหตุ: ห้าม commit ไฟล์ .env จริงขึ้น repository